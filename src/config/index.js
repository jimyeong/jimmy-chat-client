import { AppColorTheme } from './colors';
const uiConfig = {
  shadow: '1px 1px 7px 2px rgb(0 0 0 / 10%)',
  borderRadius: '8px',
  Chat: {
    ChatWindowHeight: '700px',
  },
};

export { uiConfig, AppColorTheme };
